// Central place for NESTORA's brand + agent details.
// Edit these values to update every generated post at once.
// Object.freeze guarantees these values are constant at runtime: no user
// input, prop, or future code change can reassign or mutate them. Nothing
// in the app ever passes property-input data into these fields — they are
// only ever read from here.
export const BRAND = Object.freeze({
  name: "NESTORA",
  subtitle: "PROPERTY CONSULTANTS",
  consultantName: "Surya Savarni.R",
  role: "Property Consultant",
  initials: "SS",
  // Auto-generated professional contact line — no typing required from the user.
  contactLine: "Schedule a Private Viewing",
  contactHandle: "nestora.realty",
});
